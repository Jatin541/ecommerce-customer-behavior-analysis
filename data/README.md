# Data

The source CSV is not included in this GitHub-ready package because the original project archive did not contain
a reliable source/license citation for redistribution.

Expected local filename:

`Customer_Behavior_Data.csv`

The notebook expects the file at:

`data/Customer_Behavior_Data.csv`

Before publishing the dataset itself, verify the original source and license. The repository can be published without
the raw CSV; the README and generated figures still document the project.
